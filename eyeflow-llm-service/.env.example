# LLM Service Configuration
# NOTE: LLM configuration (provider, model, API keys) is fetched from NestJS /api/llm-config/default
# This .env file only contains service infrastructure settings

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
DEBUG=False
LOG_LEVEL=INFO

# NestJS Integration
NESTJS_SERVER_URL=http://localhost:3000
USER_ID=system

# Configuration Cache TTL (minutes)
CONFIG_FETCH_INTERVAL_MINUTES=60
CONTEXT_FETCH_INTERVAL_MINUTES=60

# Notes:
# - LLM provider and model are managed in NestJS via POST /api/llm-config
# - LLM config is automatically fetched and cached
# - You can override fetch config manually via POST /config/refresh endpoint
# - LLM API keys are stored securely in NestJS database (encrypted)

