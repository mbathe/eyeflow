/// Generated Protobuf types — included from prost build output (OUT_DIR).
///
/// Access these types as `crate::proto::llmir::LlmIntermediateRepresentation`, etc.

pub mod llmir {
    include!(concat!(env!("OUT_DIR"), "/llmir.rs"));
}
